"use strict"

const randomNumP1 = () => {
  const numGenerator = Math.floor((Math.random()*6)+1);
    console.log(numGenerator)
    return numGenerator
}

const randomNumP2 = () => {
    const numGenerator = Math.floor((Math.random()*6)+1);
    console.log(numGenerator)
    return numGenerator
}